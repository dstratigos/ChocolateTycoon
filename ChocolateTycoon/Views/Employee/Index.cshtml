@model ICollection<ChocolateTycoon.Models.Employee>


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">

    <tr>
        <th>FullName</th>
        <th>Position</th>
        <th>Salary</th>
        <th>Working place</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.FullName</td>
            <td>@item.Position</td>
            <td>@item.Salary</td>
            <td>
                @if (item.FactoryID != null)
                {
                    @Html.DisplayFor(modelItem => item.Factory.Name)<text> Factory</text>
                }
                else if (item.StoreID != null)
                {
                    @Html.DisplayFor(modelItem => item.Store.Name)<text> Store</text>
                }
                else
                {
                    @Html.ActionLink("Assign to job", null, null, new { @class = "btn btn-link text-danger", @style = "padding: 0" })
                }
            </td>
        </tr>
    }












</table>

