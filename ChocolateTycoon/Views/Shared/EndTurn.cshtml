@model ChocolateTycoon.Models.Turn

<script src="@Url.Content("~/Scripts/jquery-3.4.1.min.js")" type="text/javascript"></script>
<script>
    $(document).ready(function () {
        let button = $('#endTurnBtn');
        let modelMessage = '@ChocolateTycoon.Models.Turn.TurnMessage';


        $(button).on('click', function () {

            //bootbox.confirm({
            //    message: function () {
            //        if (modelMessage == "") {
            //            return "Are you sure?"
            //        }
            //        return "factories";
            //    },
            //    callback: function (result) {
            //        if (result) {
            //            console.log('yes!');
            //        }
            //    }
            //})
        })
    })
</script>
