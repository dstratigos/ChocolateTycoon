@model ChocolateTycoon.Core.Models.Safe

    <span class="navbar-brand" id="moneyBalance">
        <span id="count">@Model.Deposit</span>
        <span class="hidden">
            @Html.ActionLink( "+", "CheatDeposit", "Safe", null, new { } )
        </span>
        <span class="hidden">
            @Html.ActionLink( "-", "CheatWithdraw", "Safe", null, new { } )
        </span>
        <i class="fas fa-coins"></i>
    </span>